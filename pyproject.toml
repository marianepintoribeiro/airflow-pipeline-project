[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "airflow-pipeline-project"
version = "0.1.0"
dependencies = [
    "pandas"
]

[tool.setuptools]
packages = ["src"]